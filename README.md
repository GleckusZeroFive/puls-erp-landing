# Puls — лендинг

Одностраничный сайт-витрина продукта Puls: показывает продукт и
собирает заявки на ранний доступ.

Сделан на **Astro + Tailwind** — это обычные HTML/CSS/JS на выходе, без
привязки к платформе или конструктору. Поддерживать может любой
веб-разработчик; никаких закрытых инструментов не используется.

---

## Что нужно установить

- **Node.js** версии 22.12 или новее — <https://nodejs.org> (LTS).
  Проверить: `node -v`.

Больше ничего ставить не нужно — все библиотеки подтянутся одной
командой ниже.

---

## Запуск на своём компьютере

```bash
npm install      # один раз — скачать зависимости
npm run dev      # запустить локально
```

После `npm run dev` в консоли появится адрес вида
`http://localhost:4321/puls-erp-landing` — открыть его в браузере.
Правки в файлах применяются автоматически, перезагружать не нужно.

---

## Сборка готового сайта

```bash
npm run build
```

Собранный сайт кладётся в папку **`dist/`**. Это обычные статические
файлы — их можно положить на любой хостинг (см. раздел «Перенос на свой
сервер»).

Проверить сборку локально перед выкладкой:

```bash
npm run preview
```

---

## Где сайт живёт сейчас

Сейчас сайт опубликован на **GitHub Pages под аккаунтом исполнителя** —
это бесплатный хостинг, нужен только чтобы у вас всегда была рабочая
ссылка на время доработок. **Своего сервера для этого не требуется,
доступы открывать не нужно.**

Адрес: `https://gleckuszerofive.github.io/puls-erp-landing/`

Деплой автоматический: при каждом изменении в основной ветке проекта
сайт пересобирается и обновляется сам (настроено в
`.github/workflows/deploy.yml`). Адрес приёмника формы при сборке
берётся из защищённой настройки репозитория, в коде его нет.

> Это временное решение на период доработок. Когда продукт созреет —
> сайт переносится на ваш сервер и ваш домен (раздел ниже). Никакой
> зависимости от исполнителя или GitHub при этом не остаётся.

---

## Форма заявок

Заявки с сайта уходят:

- в **Google-таблицу** (полный архив, выгрузка в Excel);
- на **почту** уведомлением о каждой новой заявке;
- (опционально) дублем в **Telegram** — сейчас не подключено, при
  необходимости включается без переборки кода.

Сама форма — часть сайта, а приёмник заявок вынесен в отдельный
маленький бесплатный сервис. Как он устроен, как сменить
почту/получателя и как развернуть заново — подробно описано в
**[`_server/README.md`](./_server/README.md)**.

Адрес приёмника задаётся одной строкой в файле `.env` (шаблон —
в `.env.example`):

```
PUBLIC_FORM_WEBHOOK_URL=https://script.google.com/macros/s/.../exec
```

Если строку оставить пустой — форма не ломается, а предлагает
отправить заявку письмом.

---

## Перенос на свой сервер и домен

Когда будете готовы переехать на собственный хостинг и домен
(например, `puls-erp.ru`):

1. **Изменить адрес сайта в настройках.** В файле `astro.config.mjs`:
   - `site` — поменять на ваш домен, например `https://puls-erp.ru`;
   - `base` — **удалить строку** (или поставить `'/'`). Сейчас там
     `'/puls-erp-landing'` только потому, что бесплатный хостинг кладёт
     сайт в подпапку. На своём домене сайт лежит в корне, подпапка не
     нужна — если это не убрать, картинки и стили «отвалятся». Это
     самый частый промах при переезде, поэтому отдельно подчёркиваю.

2. **Собрать:** `npm run build` → папка `dist/`.

3. **Выложить `dist/` на хостинг.** Подойдёт любой обычный хостинг для
   статических сайтов (на РЕГ.ру это тариф «хостинг», отдельный сервер
   для статики не обязателен). Содержимое папки `dist/` копируется в
   корневую папку сайта на хостинге (обычно `public_html` или `www`).

4. **Привязать домен.** В панели хостинга домен `puls-erp.ru`
   направляется на эту площадку. Если домен и хостинг у одного
   провайдера (РЕГ.ру) — это пара кликов в их панели; инструкция есть
   у них в разделе «Домены». HTTPS у большинства хостингов включается
   автоматически (бесплатный сертификат Let's Encrypt).

5. **Перенести приёмник формы** при желании — см. `_server/README.md`,
   раздел «Что когда-нибудь стоит улучшить» (свой домен для отправителя
   писем).

> Сервер с базой данных нужен продукту (ERP), а не лендингу. Лендинг —
> это набор готовых файлов, ему достаточно самого простого статического
> хостинга.

---

## Перенос в GitLab (по желанию)

Если код лендинга нужно держать рядом с остальным проектом в GitLab:

```bash
git remote set-url origin <адрес-вашего-GitLab-репозитория>
git push -u origin main
```

Автодеплой на GitHub Pages при этом перестанет работать (он привязан к
GitHub) — но это и не нужно после переезда на свой хостинг: там сайт
обновляется выкладкой папки `dist/` (пункты выше). При желании в GitLab
можно настроить аналогичный автодеплой через GitLab CI — это отдельная
небольшая задача.

---

## Независимость и передача другому разработчику

Сайт намеренно сделан так, чтобы его можно было поддерживать и развивать
без участия первоначального исполнителя.

- **Обычный стек.** Astro + Tailwind, на выходе — простые статические
  файлы. Поддержит любой фронтенд-разработчик; специальных знаний,
  закрытых инструментов или конкретного человека не требуется.
- **Никаких чужих учёток для работы сайта.** Для запуска, сборки и
  показа сайта не нужны платные сервисы, API-ключи или аккаунты
  исполнителя. Единственный внешний адрес — приёмник формы — задаётся
  одной строкой в `.env` и при необходимости пересоздаётся вашей
  стороной самостоятельно (см. [`_server/README.md`](./_server/README.md)).
- **Данные — ваши с первого дня.** Архив заявок лежит в вашей
  Google-таблице (владелец — ваш аккаунт), а не у исполнителя. Доступ
  исполнителя к ней при желании отзывается в правах таблицы, сбор заявок
  при этом не прекращается.
- **Что сейчас на стороне исполнителя.** Только временная бесплатная
  ссылка-превью (GitHub Pages под аккаунтом исполнителя). Это
  убирается полностью за три шага:
  1. перенести репозиторий к себе (раздел «Перенос в GitLab») — код
     целиком переходит под ваш контроль;
  2. собрать и выложить сайт на свой хостинг и домен (раздел «Перенос
     на свой сервер»);
  3. при желании передеплоить приёмник формы под свой служебный аккаунт
     ([`_server/README.md`](./_server/README.md)).

  После этих шагов работающий сайт не касается ни одной учётки
  исполнителя.
- **Передача новому разработчику.** Достаточно дать ему доступ к
  репозиторию и этот файл. Точка входа — `npm install` и `npm run dev`,
  дальше всё описано здесь. Ничего дополнительно запрашивать у
  первоначального исполнителя не нужно.

---

## Структура проекта

```
production-site/
├── src/
│   ├── pages/index.astro       — сборка всех секций лендинга
│   ├── components/             — секции (Hero, дашборд, форма и т.д.)
│   ├── data/                   — контент секций (роли, сценарии, FAQ и т.д.)
│   ├── layouts/Layout.astro    — общий каркас страницы, мета-теги
│   └── styles/global.css       — палитра и шрифт продукта
├── public/                     — картинки, логотип, экраны продукта
├── _server/                    — приёмник формы заявок (+ своя инструкция)
├── astro.config.mjs            — адрес сайта (site / base) — см. «Перенос»
└── .env                        — адрес приёмника формы (не в репозитории)
```

Палитра и шрифт заданы в одном месте — `src/styles/global.css`:

- `#202F4D` — основной цвет (тёмно-синий);
- `#2D61C7` — акцент (синий, кнопки);
- `#FFFFFF` фон, `#747476 / #D0D0D0 / #EFEFEF` — серые;
- шрифт **Onest**.

Тексты секций — в начале `src/pages/index.astro` и в файлах
`src/data/`.

---

## Как вносить правки безопасно

Большинство правок не требуют знания сборки — это просто текст в
понятных местах:

- **Тексты секций** — массивы в начале `src/pages/index.astro`.
- **Контент секций** (роли, сценарии, FAQ) — файлы в `src/data/`.
- **Цвета и шрифт** — одно место, `src/styles/global.css` (поменяли
  значение цвета — изменилось везде).
- **Картинки и экраны продукта** — папка `public/`.

Несколько правил, чтобы ничего не «рассыпалось»:

1. **Не трогать `base` в `astro.config.mjs`, пока сайт на бесплатном
   хостинге.** Эта строка отвечает за пути к картинкам и стилям. Менять
   её — только при переезде на свой домен, по инструкции из раздела
   «Перенос на свой сервер».
2. **Перед выкладкой собрать и проверить:** `npm run build` должен
   пройти без ошибок, затем `npm run preview` — открыть и глазами
   убедиться, что всё на месте. В папке `_qa/` лежат вспомогательные
   скрипты для автоматической проверки вёрстки на разных экранах
   (`node _qa/breakpoints.js`, `node _qa/screenshot.js`) — их результаты
   складываются в `_qa/out/` и в репозиторий не попадают.
3. **Главная ветка = то, что в проде.** Пока сайт на автодеплое, любое
   изменение в основной ветке само уезжает на сайт через несколько
   минут. Мелкие правки текста можно вносить прямо в неё; крупные
   изменения безопаснее делать в отдельной ветке и сливать после
   проверки.

Если правка касается только текста или цвета — достаточно изменить
значение в нужном файле, собрать и выложить. Разбираться в устройстве
сборки для этого не нужно.
